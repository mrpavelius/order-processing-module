name: server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.4.0
  stormberry: ^0.14.0
  db:
    path: packages/db
  shared:
    path: packages/shared

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
