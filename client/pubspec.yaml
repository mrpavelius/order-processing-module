name: order_processing_module
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  collection: ^1.18.0
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared:
    path: ../server/packages/shared
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  fonts:
    - family: Manrope
      fonts:
        - asset: lib\fonts\manrope\Manrope-Regular.ttf
          weight: 500
  uses-material-design: true
